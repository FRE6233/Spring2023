# NOTES

## Git workflow

For homework create a branches `hw`n and `work` at current `HEAD:main` 

Switch to `work` and do not commit to `HEAD::main` so it will point at `hw`n.

Add solutions to `hw`n then merge `work` into `main`. Create branch `hw`n+1.